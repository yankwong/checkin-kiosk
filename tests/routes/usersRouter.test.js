const assert = require('chai').assert;
const expect = require('chai').expect;
const sinon = require('sinon');
const checkInRouter = require('../../routes/checkInRouter');

describe('CheckIn routes tests', () => {

    context('addCheckin', () => {
        it('should add a checkin');
    });

    context('deleteCheckin', () => {
        it('should delete a checkin');
    });

    context('editCheckin', () => {
        it('should edit a checkin');
    });
});